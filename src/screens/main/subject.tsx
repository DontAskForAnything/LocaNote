import { RootStackScreenProps } from "../../types/navigation";
import { Text } from "react-native";

export const SubjectScreen = ({}: RootStackScreenProps<"SubjectScreen">) => {
  return <Text>Yeah, thats me SubjectScreen...</Text>;
};
